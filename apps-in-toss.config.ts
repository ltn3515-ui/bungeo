import { defineConfig } from '@apps-in-toss/web-framework/config';
// Register the exact immutable appName in the Apps in Toss console before upload.
export default defineConfig({appName:'bungeo-town',brand:{primaryColor:'#e87039'},webView:{},permissions:[],webBundleDir:'dist'});
